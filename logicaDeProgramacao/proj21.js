const frutas = ['Pera', 'Maçã', 'Uva'];

// for (let i = 0; i < frutas.length; i++) {
//   console.log(frutas[i]);
// }

for (let indice in frutas) { //for in -> lê os indices ou chaves de um objeto
  console.log(frutas[indice]);
}
