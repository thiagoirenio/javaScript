const pessoa = {
  nome: 'THIAGO',
  sobrenome: 'IRENIO'
};

const chave = 'nome';
console.log(pessoa['nome']); //anotação de cochetes
console.log(pessoa.sobrenome); //anotação de pontos
console.log(pessoa[chave]); //anotação de variavel
