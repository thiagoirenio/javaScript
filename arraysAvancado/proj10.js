/* shift - add no começo
 * unshift - remove do começo
 * push - add no final
 * pop - remvove do final
 * slice - add ou remove um indice de um array
 */
const nome = ['Thiago', 'Irênio', 'Caldeira'];
nomes.splice(4);
console.log(nomes);
