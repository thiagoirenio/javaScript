/* shift - add no começo
 * unshift - remove do começo
 * push - add no final
 * pop - remvove do final
 * slice - add ou remove um indice de um array
 */
const nomes = ['filho', 'pai', 'mae', 'Wallace', 'Rosana'];
const novo = nodes.slice(1, -3);
console.log(novo);
